import * as React from 'react';
import './App.css';
import TodoList from '../TodoList/TodoList';

class App extends React.Component {
  render() {
    return (
      <TodoList/>
    );
  }
}

export default App;
